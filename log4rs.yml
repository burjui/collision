refresh_rate: 1 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{l} {m}{n}"

  file:
    kind: file
    path: "collision.log"
    encoder:
      pattern: "{l} {m}{n}"

root:
  level: info
  appenders:
    - stdout
    - file
